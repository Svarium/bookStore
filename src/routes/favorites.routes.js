//Manejar los endpoints para agregar y quitar de favs los productos
const express = require("express");

const router = express.Router();
//Llego con /favs/ - esta es la ruta raíz de este enrutador





module.exports = router;